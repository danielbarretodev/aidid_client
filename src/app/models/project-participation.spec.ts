import { ProjectParticipation } from './project-participation';

describe('ProjectParticipation', () => {
  it('should create an instance', () => {
    expect(new ProjectParticipation()).toBeTruthy();
  });
});
